#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n;
    cin >> n;
    vector<int> v;
    while (n--) {
        int temp;
        cin >> temp;
        v.push_back(temp);
    }
    for (int i = 0; i < n; i++) {
    }
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    int >> t;
    while (t--) {
        solve();
    }
}
